(I'm sure Dr. Ray or someone can suggest a better way to word this)

Your connection request was not submitted, as you have already submitted 2 connection requests in the last 30 days.  It is important that a patient doesn't work with too many therapists.  

<% if @show_form %>
  If you are concerned about this policy, please fill out this form to message the site administrators.

  <%= form_tag({controller: "super_admins", action: "message"}, method: "post")do %>
    <%= hidden_field_tag :patient_id, @patient.id %>
    <%= text_area_tag "body" %>
    <%= submit_tag %>
  <% end %>
<% end %>
