<%= form_for(@patient_message) do |f| %>
  <% if @patient_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_message.errors.count, "error") %> prohibited this patient_message from being saved:</h2>

      <ul>
      <% @patient_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
