<p id="notice"><%= notice %></p>

<h1>Convesation With <%= @therapist.username %></h1>

<%= form_tag patient_reply_to_message_path, method: "post" do %>

  <%= hidden_field_tag :therapist_id, @therapist.id %>
  <%= hidden_field_tag :patient_id, @patient.id %>
  <%= hidden_field_tag :message_id, @message.id %>
  
  <%= text_field_tag :body %>

  <%= submit_tag "Send" %>

<% end %>

<table>
  <tbody>
    <% @patient_messages.each do |patient_message| %>
      <tr>
        <td>
        <% if patient_message.sent_messageable_type == "Therapist" %>
          <%= @therapist.username %>:
        <% else %>
          <%= @patient.username %>:
        <% end %>
      </td>
      <td>
        <%= patient_message.body %>
      </td>
      </tr>
    <% end %>
  </tbody>
</table>
